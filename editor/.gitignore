node_modules/
dist/
coverage/
*.tsbuildinfo

# The oracle's output: generated by `pnpm oracle` from the repository's tools (plan §0.5).
tests/oracle/out/

# The vendored schemas, corpus, reference base and generated artifacts: produced by
# `pnpm vendor` from the repository this editor is part of (plan §1, §7 F5), never committed.
apps/web/public/vendor/

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/
