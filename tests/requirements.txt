# The environment the repository's evidence is produced in (README §3, docs/TENSORSPINE-FIXTURE.md §5):
# the versions of the box the fixtures were recorded on, 6 Sep 2026, Python 3.11. The language suites
# need jsonschema alone. torch is the CPU build in CI:
#   python3 -m pip install -r tests/requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
jsonschema==4.25.0
numpy==2.3.5
safetensors==0.8.0
torch==2.13.0
transformers==5.14.1
onnx==1.22.0
onnxruntime==1.29.0
