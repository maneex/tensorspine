{
  "schema": "tensorspine-catalog-unit/2.0",
  "kind": "axis",
  "name": "ffn.gate_flat",
  "definition": {
    "space": "value",
    "summary": "Fused gate and up projection width, `2 * inner`.",
    "description": "An exact product of the `ffn.projection` and `ffn.inner` axes, hence declared factors: the fused axis can be partitioned along `inner`.",
    "note": "fused gate and up: exact product projection x inner, hence factors"
  }
}
